export enum TaskStatus {
  ACTIVE = "active",
  PAUSED = "paused",
  FINISHED = "finished",
  DELETED = "deleted",
}

export enum TaskStatusLabel {
  active = "En proceso",
  paused = "Pausada",
  finished = "Finalizada",
  deleted = "Eliminada",
}
